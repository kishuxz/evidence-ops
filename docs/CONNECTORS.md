# Connectors

The networked CourtListener/RECAP adapter is blocked (ADR 0008).

D1 provides a fixture connector and immutable snapshots only. Fixture opinions are
synthetic and must not be described as CourtListener data.

See `@evidenceops/connector` and ADR 0012.
