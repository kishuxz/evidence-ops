# `@evidenceops/cli`

Locally packaged `evidenceops` executable for this repository.

This package is **`private: true`**. It is **not published** to the npm registry.
A tarball produced by `pnpm pack` is a packaging artifact for empty-consumer tests
and private demos, not a Z2 release.

See `docs/CLI.md`.
