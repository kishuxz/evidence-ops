"""EvidenceOps Legal Python namespace.

Worker, parser, retrieval, and evaluation code lands in later execution-graph
nodes. This package exists so the F0 toolchain can install and test Python
without inventing product behavior.
"""

__version__ = "0.0.0"
