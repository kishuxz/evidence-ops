# EvidenceOps Legal agent entrypoint

Read `AGENTS.md` completely, then every file in the required reading order. Inspect the
repository, issues, PRs, branches, CI, and current implementation before planning.

Execute the dependency graph in `docs/AUTONOMOUS_EXECUTION.md`; do not implement the entire
roadmap in one branch. The immediate product direction is continuous federal case-law
research with claim-level citation integrity and temporal authority monitoring.

Do not reuse Provenance Guard types by copying them. Keep EvidenceOps contracts local until
Provenance Guard publishes a stable versioned integration surface, then integrate through
an adapter.
